{
    "name": "marai-al-rabea",
    "private": true,
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "deploy": "npm run build && gh-pages -d dist -b gh-pages"
    },
    "devDependencies": {
        "gh-pages": "^6.1.1",
        "vite": "^5.1.4"
    },
    "dependencies": {
        "lucide": "^0.577.0"
    }
}
